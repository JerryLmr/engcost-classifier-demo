Always use `backend/.venv` for backend Python commands.
Prefer `backend/.venv/bin/python` or activate with `cd backend && source .venv/bin/activate`.
Never use system Python for backend validation unless explicitly requested.
